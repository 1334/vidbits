<div class="contents-container">
  <div class="video-card">
    <a href="/videos/{{video._id}}/edit" id="edit" class="input-button">Edit</a>
    <form action="/videos/{{video._id}}/delete" method="post">
      <input type="submit" name="delete" id="delete" value="Delete" class="input-button" />
    </form>

    <div class="video-title">
      <h1>{{ video.title }}</h1>
    </div>

    <iframe class="video-player" src="{{video.url}}" frameborder="0" allowfullscreen></iframe>

    <div class="video-description">
      <p>{{ video.description }}</p>
    </div>
  </div>
</div>
